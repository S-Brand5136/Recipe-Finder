import React, { useEffect } from "react";
import { useDispatch } from "react-redux";

// MaterialUI imports
import {
  Button,
  Breadcrumbs,
  Link,
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableHead,
  TableRow,
  Typography,
} from "@material-ui/core";

// Todo: Use useEffect and dispatch to get mealID on page load

const RecipePage = () => {
  return <div> Hello </div>;
};

export default RecipePage;
